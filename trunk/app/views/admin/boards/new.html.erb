<%= page_title '새 게시판' %>

<% form_for(@board, :url => admin_boards_path) do |f| %>
	<%= render :partial => 'form', :object => f %>
  <p>
    <%= f.submit "확인" %>
  </p>
<% end %>

<%= link_to 'Back', admin_boards_path %>
